Словари:
Имеет вид ключ: значение, оборачивается в {}

Можно создать словарь используя функцию dict()
словарь = dict (ключ='значение', ....)

Добавить новое ключ: значение в словарь:
словарь['ключ']='значение'

Проверить, существуе ли ключ в словаре: словарь.get('Ключ'). Если ключа нет, вернет false, если существует True;

Объединение словарей: {**словарь1, **словарь2} - обязательно указывать ** перед название словаря.

Есть функция update для копирования ключ:значение из одного словаря в другой. Словарь1.update(словарь2). В словарь1 скопируются значения из словаря2

del словарь1['ключ'] - удалять элемент из словаря по ключу
pop возвращает ключ:значение из словаря, если оно существует и затем удаляет. словарь1.pop('Ключ')